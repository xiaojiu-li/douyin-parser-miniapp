# 抖音解析小程序 - 设计思考

## 气质与意象

- **关键词**：轻快、科技感、内容消费、工具感
- **意象锚点**：想象一个「透明水晶工具箱」—— 简洁的玻璃质感的操作界面，内部透出微光。用户把抖音链接像投入窗口一样放进去，瞬间解析出结果，干净利落。
- **场景画面**：深色背景中，一个简洁的输入框像发光窗口，用户粘贴链接后，卡片像魔法般展开，展示视频封面、标题和下载入口。

## 视觉策略

- **图像方向**：主要展示视频封面截图，圆形用户头像，保证内容本身成为视觉焦点
- **图形语言**：圆角大卡片（16px圆角）、柔和阴影、渐变色强调元素
- **整体氛围**：深底色（接近黑色）让内容更突出，粉色/品红色渐变为品牌主色

## 配色方案

| 用途 | 色值 | Tailwind 类名 | 意象来源 |
|------|------|---------------|----------|
| 背景 | `#0f0f0f` | `bg-[#0f0f0f]` | 黑暗舞台，让内容发光 |
| 卡片背景 | `#1c1c1e` | `bg-[#1c1c1e]` | 磨砂玻璃质感 |
| 主色/品牌 | `#fe2c55` | 经典抖音红 | 抖音品牌色 |
| 辅色/渐变终点 | `#25f4ee` | 青色 | 科技感与活力 |
| 文字主色 | `#ffffff` | `text-white` | 高对比度 |
| 文字次要 | `#a0a0a0` | `text-[#a0a0a0]` | 辅助信息 |
| 输入框背景 | `#2a2a2c` | `bg-[#2a2a2c]` | 深色输入区域 |
| 边框 | `#38383a` | `border-[#38383a]` | 细腻分割线 |

## 字体排版

- **设计风格**：干净、现代、高可读性
- **层级**：
  - H1 (页面标题): `text-xl font-bold text-white`
  - H2 (视频标题): `text-base font-semibold text-white`
  - Body (正文): `text-sm text-[#a0a0a0]`
  - Caption (辅助文字): `text-xs text-[#6b6b6b]`

## 间距系统

- **页面边距**: `px-4`
- **卡片内边距**: `p-4`
- **组件间距**: `gap-3` / `gap-4`
- **列表项间距**: `space-y-3`

## 组件使用原则

- **通用 UI 组件优先使用 `@/components/ui/*`**
- 按钮：使用 `Button` 组件
- 输入框：使用 `Input` 组件（经 View 包裹处理跨端）
- 卡片：使用 `Card`, `CardContent` 组件
- 加载态：使用 `Skeleton` 组件
- 提示：使用 `Toast` 组件（Sonner）
- 图标：使用 `lucide-react-taro`
- 禁止用 `View/Text` 手搓这些通用组件

## 页面结构

**首页（唯一页面）**：

1. **顶部区域**：品牌标题 + 简要说明
2. **核心交互区**：链接输入框 + "解析"按钮
3. **结果展示区**：（解析成功后显示）
   - 视频封面大图
   - 视频标题
   - 作者信息（头像 + 昵称）
   - 视频信息（点赞、播放量等）
   - "观看视频" 按钮
   - 复制无水印链接按钮
4. **历史记录区**：（可选）最近解析记录

## 动效与交互

- **按钮点击**：轻量缩放反馈
- **结果出现**：从输入框下方淡入展开
- **加载状态**：按钮显示 loading，结果区显示骨架屏
- **整体气质**：快速、流畅、不拖沓

## 设计禁忌

- ❌ 不使用过多花哨的渐变背景
- ❌ 避免白色背景（小红书风格不适合工具类）
- ❌ 不要使用 emoji 作为图标替代
- ❌ 避免过于拥挤的布局
- ❌ 不使用圆角小于 8px 的卡片
